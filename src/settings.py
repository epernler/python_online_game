# Define some colors
BLACK = (0, 0, 0)
WHITE = (255, 255, 255)
BLUE = (70, 130, 180)

# Open a new window
size = (700, 500)

# The loop will carry on until the user exits the game (e.g. clicks the close button).
carryOn = True