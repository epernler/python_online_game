import socket
import time
import threading

PORT = 1337
FORMAT = "utf-8"
HOST = socket.gethostbyname(socket.gethostname())
SERVER = socket.socket(socket.AF_INET, socket.SOCK_STREAM)  #

def send_msg()

def init_client()

